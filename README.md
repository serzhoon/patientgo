# 🏥 Веб-приложение для записи пациентов на приём
 
Курсовая работа по дисциплине «Интернет-технологии».
 
**Тема:** Разработка веб-приложения для записи пациентов на приём.
 
🔗 **Рабочая версия (развёрнута на SberCloud):** http://82.202.158.155
 
> Учебный проект. Сервер может быть доступен не круглосуточно.
 
---
 
## 📋 Описание
 
Веб-приложение позволяет пациентам записываться на приём к врачам, просматривать
и отменять свои записи, а сотрудникам регистратуры (администраторам) — видеть все
записи пациентов. Реализованы регистрация, вход и разграничение прав по ролям.
 
### Возможности
 
- Регистрация и вход по email и паролю.
- Две роли: **пациент** и **администратор (регистратура)**.
- Запись на приём с выбором врача, даты и времени.
- Защита от двойной записи: одно время у врача нельзя занять дважды.
- Просмотр и отмена записей.
- Администратор видит все записи всех пациентов.
---
 
## 🛠️ Технологический стек
 
| Слой        | Технология                          |
| ----------- | ----------------------------------- |
| Frontend    | HTML, CSS, JavaScript (без фреймворков) |
| Веб-сервер  | Nginx (отдаёт сайт и проксирует API) |
| Backend     | Node.js, Express                    |
| База данных | MySQL 8.0                           |
| DevOps      | Docker, Docker Compose              |
| Хостинг     | SberCloud (Ubuntu), GitHub Pages    |
 
---
 
## 🏗️ Архитектура
 
Приложение состоит из трёх контейнеров Docker, запущенных на сервере SberCloud:
 
```
                  Интернет
                     │
            ┌────────▼─────────┐
            │  Nginx (порт 80) │   ← отдаёт сайт, проксирует /api
            │   контейнер      │
            │  clinic-frontend │
            └───┬──────────┬───┘
                │          │ /api
       файлы сайта    ┌────▼──────────┐
                      │ Express :3000 │   ← REST API
                      │  контейнер    │
                      │ clinic-backend│
                      └────┬──────────┘
                           │
                      ┌────▼──────────┐
                      │  MySQL :3306  │   ← база данных
                      │  контейнер    │
                      │  clinic-mysql │
                      └───────────────┘
```
 
---
 
## 📁 Структура проекта
 
```
patientgo/
├── backend/            # серверная часть (Express + MySQL)
│   ├── src/
│   │   ├── config/     # подключение к базе данных
│   │   ├── controllers/# логика обработки запросов
│   │   ├── middleware/ # проверка пользователя и роли
│   │   ├── routes/     # маршруты API
│   │   └── app.js      # точка входа
│   ├── db/schema.sql   # создание таблиц и демо-данные
│   └── Dockerfile
├── frontend/           # клиентская часть
│   ├── index.html
│   ├── css/style.css
│   ├── js/app.js
│   ├── config.js       # адрес API
│   ├── nginx.conf      # конфигурация веб-сервера
│   └── Dockerfile
├── docs/               # пояснительная записка
├── docker-compose.yml  # запускает все три контейнера
└── README.md
```
 
---
 
## 🚀 Запуск через Docker
 
Нужен установленный Docker. В корне проекта выполнить:
 
```bash
docker compose up --build -d
```
 
После старта:
 
- **Сайт:** `http://localhost` (порт 80)
- **API:** `http://localhost:3000/api`
- Проверка API: `http://localhost:3000/api/health` — вернёт `{"status":"ok"}`
База данных, таблицы, врачи и администратор создаются автоматически
при первом запуске из файла `backend/db/schema.sql`.
 
Остановить:
 
```bash
docker compose down
```
 
---
 
## 👤 Данные для входа

📄 Полный список всех аккаунтов (администратор и 15 врачей) — в файле [docs/accounts.md](docs/accounts.md).

**Администратор (регистратура):**
- `admin@clinic.ru` / `Admin754`

**Врачи** (логин: `фамилия@clinic.ru`), например по одному из каждой поликлиники:
- `ivanova@clinic.ru` / `Ivanova214` — терапевт, ГАУЗ СК «Городская поликлиника № 3»
- `sokolov@clinic.ru` / `Sokolov854` — травматолог, ГБУЗ СК «Городская клиническая поликлиника №6»
- `zaharov@clinic.ru` / `Zaharov704` — онколог, ГБУЗ СК «Ставропольский краевой клинический многопрофильный центр»

> ⚠️ Готовых аккаунтов пациентов нет — пациенты создаются самостоятельно через форму регистрации на сайте (вкладка «Регистрация»).
 
## 🔌 API (основные маршруты)
 
| Метод  | Путь                          | Доступ    | Назначение              |
| ------ | ----------------------------- | --------- | ----------------------- |
| POST   | `/api/auth/register`          | все       | регистрация пациента    |
| POST   | `/api/auth/login`             | все       | вход                    |
| GET    | `/api/doctors`                | все       | список врачей           |
| POST   | `/api/doctors`                | админ     | добавить врача          |
| DELETE | `/api/doctors/:id`            | админ     | удалить врача           |
| POST   | `/api/appointments`           | авторизов.| создать запись          |
| GET    | `/api/appointments`           | авторизов.| список записей          |
| PATCH  | `/api/appointments/:id/cancel`| авторизов.| отменить запись         |
 
---
 
## 🌿 Ветки репозитория
 
- `main` — стабильная версия проекта.
- `develop` — ветка разработки.
- `gh-pages` — версия фронтенда для GitHub Pages.

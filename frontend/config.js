// ============================================================
// Конфигурация фронтенда — адрес API (бэкенда).
//
// Если сайт открыт с самого сервера (через Nginx на SberCloud),
// запросы идут на /api — Nginx перенаправит их на бэкенд.
// Если открыт иначе (локально или с GitHub Pages) — полный адрес.
// ============================================================

const SERVER_API = 'http://82.202.158.155:3000/api';

const API_BASE =
  window.location.hostname === '82.202.158.155'
    ? '/api'
    : SERVER_API;
